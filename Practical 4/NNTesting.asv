function prediction = NNTesting(testImage, modelNN)

smallestDist = 999;

    distances = EuclideanDistance(testImage, modelNN.neighbours);
    
for i=1:size(distances,1)
    
    if(distances(i) < smallestDist)
         
    
    end

    prediction(i) = modelNN.labels(i);
end